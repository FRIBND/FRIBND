# FRIBND Marketplace

This directory is a standalone Git-based marketplace scaffold for local VS Code agent plugin discovery.

The marketplace manifest is in `.github/plugin/marketplace.json` and points to `plugins/fribnd`.